batch_size: 16
epoch: 100
learning_rate: 0.5
gpu: 0
num_point: 2048
optimizer: SGD
weight_decay: 0.0001
normal: True
lr_decay: 0.001
step_size: 10

defaults:
  # - model: Zhao
   - model: Hengshuang

hydra:
  run:
    dir: log/partseg/${model.name}

  sweep:
    dir: log/partseg
    subdir: ${model.name}

    
